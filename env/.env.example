NODE_ENV=development
PORT=3002
APP_NAME=FastifyApp
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=root
DB_NAME=shinr
OTP_SECRET=shinr@2025otpsecret
REFRESH_TOKEN_SECRET=shinr@2025refreshtokensecret
REFRESH_TOKEN_EXPIRY_DAYS=60
ACCESS_TOKEN_SECRET=shinr@2025accesstokensecret
DEV_OTP=1234
 
# Optional: file storage (leave empty to store files locally under ./uploads)
# For local: use your API base URL so file URLs are fetchable (match PORT below)
FILE_BASE_URL=http://localhost:3002
# For production: FILE_BASE_URL=https://yourdomain.com
# S3 (optional – if all four are set, uploads go to S3 instead of local):
AWS_S3_BUCKET=
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_PUBLIC_BASE_URL=